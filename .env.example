# Скопируй в `.env` и заполни перед docker compose up.
# Включает Basic Auth, если оба заданы. Иначе — без авторизации
# (только для запуска внутри VPN / Tailscale!).

YT2MP3_AUTH_USER=
YT2MP3_AUTH_PASS=
